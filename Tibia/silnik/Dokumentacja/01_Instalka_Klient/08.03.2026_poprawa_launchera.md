# PLAN POPRAWEK — Launcher & Bootstrap
**Data:** 08.03.2026  
**Status:** ZAKOŃCZONE (czeka na build GHA do weryfikacji A1)  
**Repozytorium:** PtakuPL/ooo, branch: feature/ticket-gate  

---

## A. BOOTSTRAP — Problemy z bieżącego testu

| # | Problem | Opis | Pliki | Status |
|---|---------|------|-------|--------|
| A1 | Bootstrap wyświetla PL zamiast wybranego języka | Stary binary (sprzed i18n push). Nowy build z GHA powinien to naprawić — zweryfikować że dialog pojawia się PRZED jakimkolwiek komunikatem | main.rs | ⏳ Czeka na test nowego buildu |
| A2 | Brak wyboru folderu instalacji | Dodano Win32 folder picker `SHBrowseForFolderW` via FFI + YES/NO MessageBox z domyślną ścieżką. `ask_install_dir()` w platform.rs | platform.rs, main.rs | ✅ DONE |
| A3 | Bootstrap config pisze twojserwer.pl | Zmieniono `write_config()` na `127.0.0.1/apik/v1` | installer.rs:99 | ✅ DONE |
| A4 | API_BASE w bootstrap = 127.0.0.1 | DEV-only, zostawione jako 127.0.0.1 (self-signed cert) — do zmiany przy produkcji | main.rs:12 | ⏳ Zostawione na produkcję |
| A5 | Bootstrap powinien pokazać ścieżkę instalacji | Dodano i18n string `installed_at` + wyświetlanie ścieżki w main.rs po write_config | main.rs, i18n.rs | ✅ DONE |

---

## B. LAUNCHER GŁÓWNY (Tauri) — Problemy ze screenów

| # | Problem | Opis | Pliki | Status |
|---|---------|------|-------|--------|
| B1 | Session Key widoczny dla gracza | Pole zmienione na `type="hidden"`, usunięte z widocznego UI | app.js, index.html | ✅ DONE |
| B2 | Mieszane PL/EN teksty | Zweryfikowano: wszystkie klucze spójne w en.json, pl.json, ar.json, he.json, fa.json. Dodano brakującą sekcję `login.*` do ar/he/fa | en.json, pl.json, ar.json, he.json, fa.json | ✅ DONE |
| B3 | Language packs fetch failed | Stworzono `language-packs.php`, wdrożono na serwer, zweryfikowano curl (HTTP 200, poprawny JSON) | language-packs.php | ✅ DONE |
| B4 | Brak pamięci logowania (persistent login) | Sesja zapisywana w localStorage. Na starcie: walidacja przez `refresh_launcher_account_context` → auto-enter lub login screen. Wylogowanie czyści sesję | app.js | ✅ DONE |
| B5 | Brak OAuth: Google, Facebook, Steam | Dodano 3 przyciski OAuth na ekranie logowania, podpięte do `oauth-start.php?provider=X&mode=login&source=launcher` | app.js, index.html | ✅ DONE |
| B6 | Złe UX logowania — brak ekranu login-first | Dodano `#screen-login` jako pierwszy aktywny ekran. Po zalogowaniu → `enterAuthenticatedMode()` odkrywa pełne UI | app.js, index.html, style.css | ✅ DONE |
| B7 | Otwieranie stron WWW z sesją konta | Przyciski Website/Rules używają `build_create_character_url` z session key. Fallback do plain URL | app.js | ✅ DONE |

---

## C. ADRESY/URL-e — Centralna mapa do podmiany przy zmianie domeny/VPS

**INSTRUKCJA:** Przy zmianie domeny/VPS zmień `{DOMENA}` na nową domenę we WSZYSTKICH miejscach poniżej.

| # | Lokalizacja | Obecne | Docelowe | Do czego | Status |
|---|------------|--------|----------|----------|--------|
| C1 | bootstrap `main.rs:12` | `https://127.0.0.1/apik/v1` | Zostawione jako 127.0.0.1 (dev) | API bootstrap | ✅ OK (dev) |
| C2 | bootstrap `installer.rs:99` | `twojserwer.pl` → `127.0.0.1` | `https://127.0.0.1/apik/v1` | Config dla launchera | ✅ DONE |
| C3 | launcher `app.js:599` | `serwercanary.pl` → `127.0.0.1` | `https://127.0.0.1` | Website, zasady, tworzenie postaci | ✅ DONE |
| C4 | launcher `launcher_config.rs:111` | `api.serwercanary.pl/client/` → `127.0.0.1/apik/v1/` | `https://127.0.0.1/apik/v1/` | Default fallback API | ✅ DONE |
| C5 | `.env` LAUNCHER_DOWNLOAD_URL | `https://127.0.0.1/files/...` | Bez zmian (poprawne) | URL-e pobierania | ✅ OK |
| C6 | `.env` FILES_BASE_URL | `https://127.0.0.1/files` | Bez zmian (poprawne) | Pliki klienta | ✅ OK |
| C7 | `.env` URL | `https://127.0.0.1` | Bez zmian (poprawne) | Główny URL serwisu | ✅ OK |
| C8 | `launcher_config.json` (runtime) | `172.29.76.234` → `127.0.0.1` | `https://127.0.0.1/apik/v1` | Runtime config launchera | ✅ DONE |

---

## D. BRAKUJĄCE ENDPOINTY API

| # | Endpoint | Status | Potrzebny przez | Priorytet |
|---|----------|--------|----------------|-----------|
| D1 | `language-packs.php` | ✅ DONE — wdrożone, zwraca JSON z 5 paczkami (pl,en,ar,he,fa) | Launcher Settings → Language packs | Wysoki |

---

## E. UX/FLOW — Nowy design logowania

| # | Problem | Opis | Status |
|---|---------|------|--------|
| E1 | Ekran logowania jako pierwszy widok | `#screen-login` jako pierwszy aktywny ekran z login form + register toggle + OAuth | ✅ DONE |
| E2 | Trwała sesja logowania | localStorage + walidacja na starcie przez `refresh_launcher_account_context`. Logout czyści sesję | ✅ DONE |
| E3 | OAuth login (Google/Facebook/Steam) | 3 przyciski na ekranie logowania → `oauth-start.php?provider=X` → otwiera przeglądarkę | ✅ DONE |
| E4 | Otwieranie WWW z sesją | Website/Rules/CreateChar używają `build_create_character_url` z session key. Fallback do plain URL | ✅ DONE |
| E5 | Ukrycie session key z UI gracza | `type="hidden"`, usunięte z widocznego UI | ✅ DONE |
| E6 | Bootstrap: wybór folderu instalacji | `SHBrowseForFolderW` FFI + YES/NO MessageBox. Domyślnie `%LOCALAPPDATA%\SerwerCanary\` | ✅ DONE |
| E7 | Bootstrap: komunikat końcowy ze ścieżką | `installed_at` i18n string + `ui::set_status` z pełną ścieżką | ✅ DONE |

---

## PRIORYTETY REALIZACJI

### Faza 1 — Krytyczne (odblokowanie testów) ✅
1. **C1-C8** — ✅ Naprawione WSZYSTKIE adresy (127.0.0.1 dla dev)  
2. **A3** — ✅ Config z prawidłowym URL-em  
3. **D1** — ✅ `language-packs.php` stworzone i wdrożone  

### Faza 2 — UX logowania (kluczowe dla graczy) ✅
4. **E1+E2** — ✅ Ekran login-first + trwała sesja  
5. **B1/E5** — ✅ Session key ukryty  
6. **E3/B5** — ✅ OAuth logowanie (Google/Facebook/Steam)  
7. **E4/B7** — ✅ Otwieranie WWW z sesją  

### Faza 3 — Bootstrap UX ✅ (A1 czeka na build)
8. **A2/E6** — ✅ Folder picker w bootstrap  
9. **A5/E7** — ✅ Komunikat ze ścieżką instalacji  
10. **A1** — ⏳ Weryfikacja i18n w nowym buildzie  

### Faza 4 — Polish ✅
11. **B2** — ✅ Pełna weryfikacja tłumaczeń (5 języków)  
12. **B4** — ✅ Persistent login zweryfikowany  

---

## NOTATKI
- Kompilacje/buildy TYLKO na końcu, po zakończeniu WSZYSTKICH zmian w kodzie
- Push na GHA → dispatch workflow → pobranie artefaktów → test na Windows
- Adresy: przy zmianie VPS/domeny wystarczy podmienić `{DOMENA}` w sekcji C
- **System deinstalacji** — pełna implementacja w osobnym planie: `08.03.2026_plan_deinstalacji_launchera.md` ✅
- **Codex** pracuje równolegle nad systemem globalnych gildii i topek (osobny branch)

## ZMIENIONE PLIKI (sesja 08-09.03.2026)
- `installer.rs` — URL fix (twojserwer.pl → 127.0.0.1)
- `app.js` — Major rewrite: login-first gate, auth functions, OAuth, session-synced URLs
- `index.html` — Added #screen-login, login/register forms, OAuth buttons, logged-user-bar
- `style.css` — Login screen styles, OAuth buttons, logged-user-bar
- `launcher_config.rs` — Default API URL fix
- `platform.rs` — Win32 SHBrowseForFolderW folder picker
- `main.rs` (bootstrap) — ask_install_dir + install path message
- `i18n.rs` (bootstrap) — choose_install_dir + installed_at strings (5 languages)
- `en.json` — Added login.* section (27 keys)
- `pl.json` — Added login.* section (27 keys)
- `ar.json` — Added login.* section (27 keys)
- `he.json` — Added login.* section (27 keys)
- `fa.json` — Added login.* section (27 keys)
- `launcher_config.json` (2 files) — API URL fixes
- `language-packs.php` — NEW endpoint (deployed to live)