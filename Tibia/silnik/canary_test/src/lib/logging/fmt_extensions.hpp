#pragma once

// fmt_extensions.hpp
//
// No longer needed — the generic format_as<E> template in pch.hpp handles
// all enum types automatically by mapping them to their underlying integral
// value.  Kept as an empty header so existing #include directives don't break.
