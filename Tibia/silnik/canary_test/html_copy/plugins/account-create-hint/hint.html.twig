Aby grać na {{ config.lua.serverName }} potrzebujesz konta.
Wystarczy podać {% if constant('USE_ACCOUNT_NAME') %}nazwę konta{% else %}numer konta{% endif %}, hasło{% if setting('core.account_country') %}, kraj{% endif %} oraz adres e-mail.
Musisz również zaakceptować regulamin przedstawiony poniżej. Po wykonaniu tych kroków {% if constant('USE_ACCOUNT_NAME') %}nazwa konta{% else %}numer konta{% endif %} zostanie wyświetlona na następnej stronie, a hasło do konta zostanie wysłane na podany adres e-mail wraz z dalszymi instrukcjami. Jeśli nie otrzymasz wiadomości z hasłem, sprawdź folder ze spamem.<br/><br/>
