# Bootstrap Launcher Downloads

Ten folder zawiera binarki lekkiego launchera (bootstrap).
Pliki są wrzucane automatycznie przez `deploy_bootstrap.sh`.

**NIE edytuj ręcznie** — skrypt deploy zarządza plikami i aktualizuje `.env`.
